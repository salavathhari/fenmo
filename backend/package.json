{
  "name": "backend",
  "version": "1.0.0",
  "description": "Expense tracker backend",
  "main": "src/server.js",
  "scripts": {
    "build": "node -e \"console.log('No build step required')\"",
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "node --test",
    "demo:idempotency": "node scripts/demo-idempotency.js"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
